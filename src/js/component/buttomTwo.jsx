import PropTypes from "prop-types";
import React from "react";

const ButtomTwo = (props) => {
  return (
    
    <button type="button" className="btn btn-warning btn-lg botones-varios" onClick={props.reanudar}>REANUDA</button>

  
  );
};

export default ButtomTwo;