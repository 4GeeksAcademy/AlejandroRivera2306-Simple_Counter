import PropTypes from "prop-types";
import React from "react";

const ButtomThree = (props) => {
  return (
    
    <button type="button" className="btn btn-success btn-lg botones-varios" onClick={props.reiniciar} >REINICIAR</button>
  
  
  );
};

export default ButtomThree;